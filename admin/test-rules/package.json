{
  "name": "biketeilen-rules-check",
  "private": true,
  "type": "module",
  "description": "firestore.rules をエミュレータで確かめる。admin 本体の依存とは分けている（重いのと、普段の node --test では使わないため）",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project biketeilen 'node roadReviews.rules.test.mjs'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0"
  }
}
